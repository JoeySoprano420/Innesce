
-- Duration conversions via `as` (constant-folds where possible)
fn main() -> i32 with [time] is
  let a: ms := 1500 ms;
  let b: sec := (a as sec);     -- dynamic (depends on a)
  let c: i32 := (1500 ms as sec); -- constant-folded to 1
  sleep(100 ms);
  return b + c;
end
