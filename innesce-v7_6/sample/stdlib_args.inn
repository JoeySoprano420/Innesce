
-- Extended stdlib with args + gates
fn main() -> i32 with [fs.open, net.tcp, rand, time] is
  let fd: i32 := fs_open("data.txt");
  let ok: i32 := net_tcp("example.com", 443);
  let r: i32 := rand_range(1, 6);
  sleep(100 ms);
  return fd + ok + r;
end
